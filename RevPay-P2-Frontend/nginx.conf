server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # This handles Angular's internal routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
